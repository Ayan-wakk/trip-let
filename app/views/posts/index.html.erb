<div class="relative text-center">
  <h1 class="font-mono text-4xl font-medium text-gray-800 py-8">投稿一覧</h1>
</div>

<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
  <% @posts.each do |post| %>
    <%= link_to post_path(post),
        class: "block rounded-xl bg-white shadow hover:shadow-lg transition overflow-hidden" do %>

      <!-- 画像 -->
      <% if post.images.attached? %>
        <%= image_tag post.images.first, class: "h-48 w-full object-cover" %>
      <% else %>
        <div class="h-48 w-full bg-gray-200 flex items-center justify-center text-gray-500">
          No Image
        </div>
      <% end %>

      <!-- 下部 -->
      <div class="p-4 flex gap-3">

        <!-- 仮アイコン -->
        <div class="h-10 w-10 rounded-full bg-gray-300
                    flex items-center justify-center
                    text-gray-600 text-lg shrink-0">
          👤
        </div>

        <!-- テキスト -->
        <div class="min-w-0">
          <p class="font-semibold text-base line-clamp-2">
            <%= post.title %>
          </p>

          <p class="mt-1 text-sm text-gray-600">
            <%= post.country %>
            <% if post.region.present? %>
              / <%= post.region %>
            <% end %>
          </p>
        </div>

      </div>

    <% end %>
  <% end %>
</div>