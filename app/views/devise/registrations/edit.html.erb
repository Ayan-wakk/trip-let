<div class="flex justify-center items-start pt-4">
  <div class="w-full sm:w-[360px] md:w-[420px]  p-8 bg-white rounded-xl shadow-2xl hover:shadow-2xl">    
    <h2 class="text-2xl font-medium text-center text-gray-800 mb-4">
      アカウント設定
    </h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: "space-y-6" }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div>
        <%= f.label :email, "メールアドレス", class: "block text-sm font-medium text-gray-700" %>
        <%= f.email_field :email,
            autofocus: true,
            autocomplete: "email", 
            disabled: resource.provider.present?, # Googleログインの場合は無効化
            class: "mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm
                    #{resource.provider.present? ? 'bg-gray-100 cursor-not-allowed' : 'focus:outline-none focus:ring-blue-500 focus:border-blue-500'}" %>

        <% if resource.provider.present? %>
          <p class="text-xs text-gray-500 mt-1">
            ※ Googleアカウントと連携しているため変更できません
          </p>
        <% end %>
      </div>

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <div class="text-xs text-orange-600 bg-orange-50 p-2 rounded">
          現在、<%= resource.unconfirmed_email %> への変更を確認中です。
        </div>
      <% end %>

      <!-- パスワード変更（通常ログインユーザーのみ表示） -->
      <% unless resource.provider.present? %>
        <div>
          <%= f.label :password, "新しいパスワード", class: "block text-sm font-medium text-gray-700" %>
          <span class="text-xs text-gray-500">(変更しない場合は空欄のまま)</span>
          <%= f.password_field :password, autocomplete: "new-password", 
              class: "mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" %>
          <% if @minimum_password_length %>
            <p class="text-xs text-gray-400 mt-1"><%= @minimum_password_length %> 文字以上必要です</p>
          <% end %>
        </div>

        <div>
          <%= f.label :password_confirmation, "新しいパスワード（確認用）", class: "block text-sm font-medium text-gray-700" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", 
              class: "mt-1 block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" %>
        </div>

        <div class="pt-4 border-t border-gray-100">
          <%= f.label :current_password, "現在のパスワード", class: "block text-sm font-bold text-gray-700" %>
          <span class="text-xs text-gray-500">(設定を保存するために必要です)</span>
          <%= f.password_field :current_password, autocomplete: "current-password", 
              class: "mt-1 block w-full px-3 py-2 border-2 border-blue-200 rounded-md shadow-sm focus:outline-none focus:ring-blue-500 focus:border-blue-500" %>
        </div>

        <div>
          <%= f.submit "更新する", class: "w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 cursor-pointer" %>
        </div>
      <% else %>
        <!-- Googleログインユーザー向けメッセージ（更新ボタンなし） -->
        <div class="pt-4">
          <div class="bg-blue-50 p-4 rounded-md">
            <p class="text-sm text-blue-800">
              <span class="font-medium">Googleアカウントでログイン中</span><br>
              パスワードはGoogleアカウントで管理されています。
            </p>
          </div>
        </div>
      <% end %>
    <% end %>

    <hr class="my-4">

    <div class="text-center">
      <h3 class="mt-2 text-2xl font-medium text-center text-gray-800">アカウントの管理</h3>

      <!-- 削除に関する注意書き -->
      <div class="mt-4 bg-red-50 text-left border border-red-200 rounded-md p-4 mb-8">
        <p class="text-sm text-red-800">
          <span class="font-medium">⚠️ 注意 ⚠️</span><br>
          アカウントを削除すると、以下のデータも完全に削除されます：
        </p>
        <ul class="text-sm text-red-700 mt-2 text-left list-disc list-inside">
          <li>すべての投稿</li>
          <li>コメント</li>
          <li>いいね</li>
          <li>その他のアカウント関連情報</li>
        </ul>
        <p class="text-sm text-red-800 mt-2 font-medium">
          この操作は取り消せません。
        </p>
      </div>
      
      <%= button_to "Trip-letからアカウントを削除する", registration_path(resource_name), 
          data: { confirm: "本当に削除しますか？", turbo_confirm: "本当に削除しますか？" }, 
          method: :delete, 
          class: "inline-block px-6 py-2 text-base font-semibold text-red-500
                  border-2 border-red-500 rounded-md transition-colors duration-200
                  hover:bg-red-500 hover:text-white" %>
    </div>

    <hr class="my-8">

    <div class="mt-6 text-center">
      <%= link_to "戻る", :back, class: "text-base text-blue-600 hover:underline" %>
    </div>
  </div>
</div>
